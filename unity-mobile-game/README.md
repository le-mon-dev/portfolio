# unity-mobile-game

Unity 2D(URP) 템플릿으로 시작한 모바일 게임 프로젝트입니다. 현재는 프로젝트 설정과 기본 씬만 있는 시작 단계입니다.

- Unity 버전: `ProjectSettings/ProjectVersion.txt` 참고
- 열기: Unity Hub에서 이 폴더를 프로젝트로 추가
- `Library/`, `Logs/`, `obj/` 등 생성 파일은 `.gitignore`로 제외
